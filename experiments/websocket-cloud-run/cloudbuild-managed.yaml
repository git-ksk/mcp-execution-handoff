steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - experiments/websocket-cloud-run/Dockerfile.managed
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
